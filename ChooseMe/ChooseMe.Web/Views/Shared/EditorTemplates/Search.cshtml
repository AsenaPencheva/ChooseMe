@using (Html.BeginForm("All", null, FormMethod.Get))
{
     @Html.TextBox("SearchString", "", new { @class = "form-control", id = "input-search", name = "searchString", placeholder = "Search by name..." })
}
